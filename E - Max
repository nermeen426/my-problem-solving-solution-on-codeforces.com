#include <iostream>
#include <vector>
using namespace std;
 
int main() {
int N ;
cin >> N ;
vector<int>arr(N);
for(int i = 0 ; i < N ; i++)
cin >> arr[i] ;
 
int max = arr[0] ;
for(int i = 1 ; i < N ; i++){
    if(max < arr[i])
    max = arr[i];}
    cout << max ;
 
 
    return 0;
}
